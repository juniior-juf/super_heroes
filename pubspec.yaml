name: super_heroes
description: Project development for test in flutter vacancy application.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  flutter_dotenv: ^5.0.2
  dio: ^5.0.2
  crypto: ^3.0.2
  provider: ^6.0.5
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  mockito: ^5.3.2
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
  assets:
    - .env
  